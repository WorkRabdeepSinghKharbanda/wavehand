---
entry_point: src/pages/ListiclePage.tsx, src/content/listicles.ts
category: Content / SEO
---

`/best/:slug` renders one `src/content/listicles.ts` entry as an `ItemList` (JSON-LD + rendered `<ol>`). WaveHand has no product catalog to build a real comparison from, so this deliberately lists the app's own real practice features (Gesture free play, Theremin ear training, Learn mode, loop-pedal practice, hand-remap settings) rather than a fabricated "best of" ranking against other products. Keep that constraint if adding more listicle entries — don't invent competitor comparisons or data that isn't verifiable from this codebase.
