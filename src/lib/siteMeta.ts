/** Single source of truth for the real, live production URL — swap this one line if a custom domain is added. */
export const SITE_URL = "https://wavehand.vercel.app"

/** Keep this in sync with index.html's <meta name="description"> — same real product description, used for sharing. */
export const SITE_TAGLINE =
  "Play chords and theremin tones with your hands using webcam tracking."
